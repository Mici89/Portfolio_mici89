# 2 分钟录屏脚本

## 0:00–0:15 说明问题

“这是一个面向企业遗留数据库的多 Agent 数据工作台。它解决字段命名不清、缺少注释以及业务人员不会写 SQL 的问题。”

展示首页标题和右上角 `离线演示模式`。

## 0:15–0:35 扫描结构

点击“下一步”，说明系统先看到的是表、字段和关系，而不是直接让模型猜业务。

强调：16 张表、256 个字段、27 条关系，以及运单、客户、结算三个业务域。

## 0:35–0:55 多轮取证

进入“多轮取证”，说明 Understanding Agent 根据字段分布和状态值生成证据 SQL，再确认 `signed_at` 是签收时间，而不是简单使用 `created_at`。

## 0:55–1:25 自然语言查询

进入“自然语言查询”，展示问题、生成 SQL、规则校验和结果表格。

说明代码会校验表字段、统计粒度、时间字段绑定和查询类型，避免模型生成无法解释的 SQL。

## 1:25–1:55 人工确认写入

进入“人工确认写入”，说明 UPDATE 不会直接执行，必须经过 Action Plan、影响预览和人工确认；确认后才提交事务，并回查影响行数。

点击“确认并执行事务”，展示成功状态。

## 1:55–2:00 收尾

“离线模式不需要数据库和模型 Key；如果需要查看真实链路，可以再配置 DeepSeek 和本地数据库运行完整版本。”
